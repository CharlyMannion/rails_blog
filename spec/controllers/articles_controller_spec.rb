require 'rails_helper'

RSpec.describe ArticlesController, type: :controller do
  # describe "GET /index" do
  #   it "responds with 200" do
  #     get :index
  #     expect(response).to have_http_status(200)
  #   end
  # end

  # describe "POST /" do
  #   it "responds with 200" do
  #
  #     article :create, params: { article: {title: "Test Title", text: "Test Body of Article"} }
  #     expect(response).to have_http_status(302)
  #   end

  #   it "creates an article" do
  #     expect { article :create, params: { article: { title: "Test title", text: "Test body of article" } } }.to change { Article.count }.by(1)
  #   end
  # end

  # describe "DELETE /" do
  #   it "deletes an article" do
  #     @article = create(:article)
  #     expect { delete :destroy, params: { id: @post.id } }.to change { Post.count }.by(-1)
  #   end
  # end
end
